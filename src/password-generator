// Import the 'generate-password' module to create a random password
const generator = require('generate-password');

// Generate a password with minum length of 10 and includes numbers
const password = generator.generate({
        length: 10,
        numbers: true,
});

// Display password in console
console.log(password);